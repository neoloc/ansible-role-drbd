# {{ ansible_managed }}
global {
	usage-count no;
}

common {
{% for section in drbd_common.keys() %}
  {{ section }} {
{%   for line in drbd_common[section].splitlines() %}
    {{ line }}
{%   endfor %}
  }
{% endfor %}
}
